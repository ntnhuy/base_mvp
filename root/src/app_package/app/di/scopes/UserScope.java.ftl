package ${packageName}.app.di.scopes;

import javax.inject.Scope;

/**
 * User: ntnhuy
 * Date: ${.now?string('M/dd/yy')}
 * Time: ${.now?string('h:mm a')}
 */
 
@Scope
public @interface UserScope {

}
